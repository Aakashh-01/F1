using UnityEngine;

public class BasicMotor : DrivetrainBrakeSystem
{
    // Existing scene/prefab references keep using this MonoScript.
    // The production drivetrain/brake behavior now lives in DrivetrainBrakeSystem.
}
